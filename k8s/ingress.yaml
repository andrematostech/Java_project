apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: gymhub-ingress
  namespace: gymhub
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: api-gateway
                port:
                  number: 8080
